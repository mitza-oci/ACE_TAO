if (NOT TARGET ACE)
  find_package(ACE REQUIRED CONFIG)
endif()

@EXTRA_CONFIG_OPTIONS@

# Compute paths
get_filename_component(TAO_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include("${TAO_CMAKE_DIR}/TAOTargets.cmake" )
include("${TAO_CMAKE_DIR}/AddTaoIdlTargets.cmake")
include("${TAO_CMAKE_DIR}/AddTaoLib.cmake")



